# DWRT manual probe artifact examples

Representative small artifacts copied from the ignored live run directory for `dwrt-manual-probe-session-20260531.md`.

These examples are intentionally trimmed/redacted:

- no ETW `.etl` files;
- no full run directory;
- only a representative injector summary and selected JSONL snapshot rows;
- local machine paths are replaced with `<repo>` / `<deadlock>` placeholders.

Files:

- `dwrt-live-injector.example.json` - final injector/session summary.
- `dwrt-manual-probe-snapshots.example.jsonl` - initial, first damage, first recursive callback, and final snapshot rows.
